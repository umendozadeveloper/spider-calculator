import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.spider',
  appName: 'Calculadora Arácnida',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
